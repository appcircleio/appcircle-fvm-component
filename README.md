# Appcircle _Install Flutter Version Manager_ component

Install Flutter with [FVM](https://fvm.app/)

## Optional Inputs

- `AC_SELECTED_FLUTTER_VERSION`: Selected Flutter Version. Specifies the Flutter version to install. Defaults to: stable. The version you set on config section will override this setting.
